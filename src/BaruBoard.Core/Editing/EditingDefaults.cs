namespace BaruBoard.Core.Editing;

public static class EditingDefaults
{
    // World-space offset applied to each pasted or duplicated element so the copy
    // does not sit exactly on top of its source.
    public const double PasteOffset = 20.0;
}
